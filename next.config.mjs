/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['mercantilatacado.vtexassets.com', 'images.tcdn.com.br', 'm.media-amazon.com', 'www.sonoma.com.br', 'a-static.mlcdn.com.br'],
  },
};

export default nextConfig;
